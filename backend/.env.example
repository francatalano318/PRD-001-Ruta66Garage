DB_HOST=localhost
DB_PORT=5432
DB_USER=ruta66
DB_PASSWORD=ruta66
DB_NAME=ruta66garage

# RF-06/RF-07: clasificación automática por IA.
# Sin esta key (o si el servicio de OpenAI falla), la orden se crea igual
# "Sin clasificar" (ver RF-22/23/24).
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# RF-01/RF-02: firma de los tokens de sesión. Sin esto, usa un default de
# desarrollo (ver auth/jwt-secret.ts) — cambiar en cualquier entorno real.
JWT_SECRET=
